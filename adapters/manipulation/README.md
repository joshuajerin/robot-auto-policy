# Manipulation Adapter Stub

Manipulation will reuse the same `TaskAdapter` interface as locomotion:

- build task-specific scenarios
- score fixed and generated evaluations
- diagnose failures
- expose a constrained editable training surface

The first implementation deliberately keeps this as a stub so the hackathon MVP
does not confuse generalized architecture with unfinished manipulation claims.
