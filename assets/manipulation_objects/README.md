# Manipulation Object Assets

This directory contains lightweight USD primitives for fast manipulation
scenario development. Full mesh datasets such as YCB are imported into
`artifacts/datasets/` and are intentionally not committed.

The primitive set is enough for AutoResearch scenario generation, config
validation, and Isaac Lab scene-builder wiring before heavier object datasets
are available.
